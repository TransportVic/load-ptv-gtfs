import { GTFS_CONSTANTS } from '@transportme/transportvic-utils'
const { TRANSIT_MODES, GTFS_MODES } = GTFS_CONSTANTS

const TIMEZONES = {
  melbourne: 'Australia/Melbourne'
}

const CALENDAR_DATES = {
  ADDED: '1',
  REMOVED: '2'
}

export {
  TRANSIT_MODES,
  TIMEZONES,
  CALENDAR_DATES,
  GTFS_MODES
}