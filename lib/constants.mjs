import { TRANSIT_MODES, GTFS_MODES } from '../transportvic-data/gtfs/constants.mjs'

const TIMEZONES = {
  melbourne: 'Australia/Melbourne'
}

const CALENDAR_DATES = {
  ADDED: '1',
  REMOVED: '2'
}

export {
  TRANSIT_MODES,
  TIMEZONES,
  CALENDAR_DATES,
  GTFS_MODES
}